function setup() {
	createCanvas(windowWidth, windowHeight);
	background(100);
}


function draw() {
	function cantor(x, y, length) {
		if (length > 1) {
		line(x, y, x+length, y);		
		y +=50;		
		cantor(x, y, length/3);
		cantor(x+length*2/3,y,length/3)
		}
		}
	cantor(50, 50, 1000)

}
